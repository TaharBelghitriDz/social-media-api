import { RequestHandler } from "express";

export const profileBio: RequestHandler = (req, resF) => {};
export const profileCover: RequestHandler = (req, resF) => {};
export const profileFollow: RequestHandler = (req, resF) => {};
